// `mod.rs` acts as the root of the `foo` module.
// Instead of using a `mod.rs` file in the module directory it's also
// possible to use a file with the same name placed at the same level
// as the directory. See `nested.rs` for example.
pub mod nested;
